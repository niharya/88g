# Scriptorium — marks

Verbatim copy reference. Edit the source files, not this doc — run `/prepush` to surface drift.

**Sources:** `app/marks/page.tsx`, `app/marks/components/*.tsx`, `app/marks/data/marks.ts`

---

## Hero — big title (decorative) {#hero-title}

> "MARKS &"
> — [`HeroText.tsx:116`](../../app/marks/components/HeroText.tsx#L116)

> "SYMBOLS"
> — [`HeroText.tsx:118`](../../app/marks/components/HeroText.tsx#L118)

## Hero — section aria-label {#hero-aria}

> "Marks & Symbols"
> — [`Hero.tsx:10`](../../app/marks/components/Hero.tsx#L10)

## Docked title — label state {#title-label}

> "MARKS & SYMBOLS"
> — [`MarksTitle.tsx:38`](../../app/marks/components/MarksTitle.tsx#L38)

## Essay — body paragraph 1 {#essay-body-1}

> "My approach to designing marks shifted from answering the brief to treating them as empty vessels, capable of carrying an idea or an emotion."
> — [`Essay.tsx:66-68`](../../app/marks/components/Essay.tsx#L66)

## Essay — body paragraph 2 {#essay-body-2}

> "The process then became about uncovering that idea or emotion first, and letting the form emerge from it."
> — [`Essay.tsx:78`](../../app/marks/components/Essay.tsx#L78)

> "Over time, a mark surfaces: one that carries the feeling and also resonates with the client."
> — [`Essay.tsx:79`](../../app/marks/components/Essay.tsx#L79)

## Essay — between-row caption {#essay-caption-between}

> "For me, marks are not decorations but transmissions."
> — [`Essay.tsx:95`](../../app/marks/components/Essay.tsx#L95)

## Essay — closing caption {#essay-caption-closing}

> "Here is a selection of some distilled ideas and emotions."
> — [`Essay.tsx:108`](../../app/marks/components/Essay.tsx#L108)

## Marks data — Aleyr (Furrmark) {#mark-furrmark}

> Name: "Aleyr"
> — [`marks.ts:53`](../../app/marks/data/marks.ts#L53)

> Year: 2021
> — [`marks.ts:54`](../../app/marks/data/marks.ts#L54)

> Story: "This mark is the face your pet makes when your hand rests on its head."
> — [`marks.ts:55`](../../app/marks/data/marks.ts#L55)

Slide captions:

> "Early iterations"
> — [`marks.ts:60`](../../app/marks/data/marks.ts#L60)

> "Refinement journey"
> — [`marks.ts:61`](../../app/marks/data/marks.ts#L61)

> "Brand-book placement rules for the mark."
> — [`marks.ts:62`](../../app/marks/data/marks.ts#L62)

> "Pages of the brandbook, turning."
> — [`marks.ts:63`](../../app/marks/data/marks.ts#L63)

> "Logo lockup"
> — [`marks.ts:64`](../../app/marks/data/marks.ts#L64)

## Marks data — Codezeros {#mark-codezeros}

> Name: "Codezeros"
> — [`marks.ts:69`](../../app/marks/data/marks.ts#L69)

> Year: 2019
> — [`marks.ts:70`](../../app/marks/data/marks.ts#L70)

> Story: "Is it read code-zeros, co-dezeros, codez-eros? The logotype solved for this ambiguity."
> — [`marks.ts:71`](../../app/marks/data/marks.ts#L71)

Slide captions:

> "Refinement"
> — [`marks.ts:77`](../../app/marks/data/marks.ts#L77)

> "First drafts"
> — [`marks.ts:78`](../../app/marks/data/marks.ts#L78)

> "Eyeballing color"
> — [`marks.ts:79`](../../app/marks/data/marks.ts#L79)

> "Just fancy animation"
> — [`marks.ts:80`](../../app/marks/data/marks.ts#L80)

> "Environmental signage at the studio’s front desk."
> — [`marks.ts:81`](../../app/marks/data/marks.ts#L81)

## Marks data — Slangbusters {#mark-slangbusters}

> Name: "Slangbusters"
> — [`marks.ts:86`](../../app/marks/data/marks.ts#L86)

> Year: 2020
> — [`marks.ts:87`](../../app/marks/data/marks.ts#L87)

> Story: "It begins with noise and ends in clarity, just like our clients going through the process."
> — [`marks.ts:88`](../../app/marks/data/marks.ts#L88)

Slide captions:

> "Early drafts"
> — [`marks.ts:94`](../../app/marks/data/marks.ts#L94)

> "Relationship trials"
> — [`marks.ts:95`](../../app/marks/data/marks.ts#L95)

> "Slangbusters logo cutout by Savan Prajapati"
> — [`marks.ts:96`](../../app/marks/data/marks.ts#L96)

> "Studio’s library stamp"
> — [`marks.ts:97`](../../app/marks/data/marks.ts#L97)

## Marks data — Oscar Beringer {#mark-beringer}

> Name: "Oscar Beringer"
> — [`marks.ts:103`](../../app/marks/data/marks.ts#L103)

> Year: 2023
> — [`marks.ts:104`](../../app/marks/data/marks.ts#L104)

> Story: "[Concept] Solves for his name’s pronunciation problem"
> — [`marks.ts:105`](../../app/marks/data/marks.ts#L105)

Slide captions:

> "Some fake mockups"
> — [`marks.ts:111`](../../app/marks/data/marks.ts#L111)

> "It’s bay-rohn-jay ffs!"
> — [`marks.ts:112`](../../app/marks/data/marks.ts#L112)

> "Like I said previously…"
> — [`marks.ts:113`](../../app/marks/data/marks.ts#L113)

> "Just"
> — [`marks.ts:114`](../../app/marks/data/marks.ts#L114)

## Marks data — Ecochain {#mark-ecochain}

> Name: "Ecochain"
> — [`marks.ts:119`](../../app/marks/data/marks.ts#L119)

> Year: 2022
> — [`marks.ts:120`](../../app/marks/data/marks.ts#L120)

> Story: "Lovechild of a typeface and a spindle of thread"
> — [`marks.ts:121`](../../app/marks/data/marks.ts#L121)

Slide captions:

> "Presentation showing how a child is born"
> — [`marks.ts:126`](../../app/marks/data/marks.ts#L126)

> "Fancy animation"
> — [`marks.ts:127`](../../app/marks/data/marks.ts#L127)

> "Real work by Savan Prajapati"
> — [`marks.ts:128`](../../app/marks/data/marks.ts#L128)

> "By Savan Prajapati + Akshar Dave"
> — [`marks.ts:129`](../../app/marks/data/marks.ts#L129)

## Marks data — Kilti {#mark-kilti}

> Name: "Kilti"
> — [`marks.ts:134`](../../app/marks/data/marks.ts#L134)

> Year: 2024
> — [`marks.ts:135`](../../app/marks/data/marks.ts#L135)

> Story: "Clothing line. Cats. Triangles."
> — [`marks.ts:136`](../../app/marks/data/marks.ts#L136)

Slide caption:

> "House of Kilti with its owner"
> — [`marks.ts:141`](../../app/marks/data/marks.ts#L141)

## MarkChrome — paginator labels {#chrome-paginator}

> "Slide pagination" (aria-label)
> — [`MarkChrome.tsx:44`](../../app/marks/components/MarkChrome.tsx#L44)

> "Go to slide {n}" (aria-label per dot, templated)
> — [`MarkChrome.tsx:74`](../../app/marks/components/MarkChrome.tsx#L74)

## MarkChrome — attribution line (templated) {#chrome-attribution}

> "{mark.name}, {mark.year}"
> — [`MarkChrome.tsx:85`](../../app/marks/components/MarkChrome.tsx#L85)

## MarksExitMarker {#exit-marker}

> "EXIT"
> — [`MarksExitMarker.tsx:24`](../../app/marks/components/MarksExitMarker.tsx#L24)

> "Back to works" (aria-label)
> — [`MarksExitMarker.tsx:25`](../../app/marks/components/MarksExitMarker.tsx#L25)

## Notes

- Slide caption is the visible caption text per slide; the first slide of each mark is `{ kind: 'mark' }` and the chrome falls back to `mark.story` for that slide ([`MarkChrome.tsx:31`](../../app/marks/components/MarkChrome.tsx#L31)).
- All curly typography characters (`’`, `…`, `&`) are preserved verbatim from source — they appear as HTML entities or literals depending on the file.
- The reel auto-restarts via HeroClone teleport; the same "MARKS & SYMBOLS" big title shows at both ends.
- MarkSection's `aria-label={mark.name}` mirrors visible text; not separately catalogued.
