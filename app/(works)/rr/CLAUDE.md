# /rr — protective digest

Part of the 88g doc family (root `CLAUDE.md` → "The document family"). Auto-loads whenever files under `app/(works)/rr/` are touched.

**Archive:** [`./ANOMALIES.md`](./ANOMALIES.md) — full rationale, what-breaks, and rejected approaches. This digest is the seatbelt; the archive is the manual. Read the archive section before structurally changing anything an item below names.

**Maintenance:** every new ANOMALIES entry gets a one-line item here in the same commit; every retired entry removes its item. The `/release` census checks this file exists and stays paired with the archive.

## Don't-touch digest

- `--rr-mech-progress` written to the stage element, never a mat — CSS variables don't cross sibling boundaries. ANOMALIES.md → "CSS variable cascade"
- Both mechanics mats fixed 678px wide, translate only, never resize — resize model clipped grid cells mid-animation. ANOMALIES.md → "The mats translate, they do not resize"
- `.is-split` visibility gate stays on the primary mat — without it the secondary's border renders as a stray line at p=0. ANOMALIES.md → "`.is-split` visibility gate"
- `#mechanics::after { display: none }` stays — the section's paper-noise pseudo would scroll with the 200vh wrapper. ANOMALIES.md → "Sticky scene pattern"
- Mechanics `handleGameOver` computes scroll target from `rect.top + window.scrollY`, never `scene.offsetTop` — resolves to 0 and scrolls backward. ANOMALIES.md → "Auto-advance on game over"
- `snap={chapter.id !== 'mechanics'}` in page.tsx is structural, never uniformize at any breakpoint — pinned scene fights snap. ANOMALIES.md → "Chapter dominance-snap (Mechanics excluded)"
- Intro's `snapIdleMs={100}` override must not extend to Cards or Outcome — near-zero idle yanks the reader during reading pauses. ANOMALIES.md → "Chapter dominance-snap (Mechanics excluded)"
- Keep the `?? sheet` fallback after `[data-arrow-target]` in `app/components/nav/useDockedMarker.ts` — other routes rely on it; this is the only global file /rr changed. ANOMALIES.md → "Global nav coupling — `[data-arrow-target]`"
- Rails open/close, board nudge, and mobile open transforms stay inline `style.transform` strings from React state — CSS-class, `:has()`, and Framer Motion transitions stall on children of `.rr-mech-family`. ANOMALIES.md → "Rails use inline `transform`, not Framer Motion or class-based CSS rules"
- Never swap the rails' inline transform string for the standalone `translate:` property — silently ignored on these elements. ANOMALIES.md → "Rails use inline `transform`, not Framer Motion or class-based CSS rules"
- Board-nudge values are a load-bearing triple (board nudge, other rail's `CLOSED_NUDGED_TRANSFORM`, opening rail's `OPEN_TRANSFORM`) — recompute together or nothing lands flush. ANOMALIES.md → "Cross-rail follow-nudge"
- Mobile rail `OPEN_TRANSFORM_MOBILE` constants never move back into CSS — they stall; recompute from bounding rects if mat width or rail base left changes. ANOMALIES.md → "Mobile open transforms live inline, not in CSS"
- `overflow: clip` (not `hidden`) on `.mat` base class and `#cards.mat` — `hidden` breaks ChapterMarker's `position: sticky`. ANOMALIES.md → "Global mat containment (`overflow: clip`)"
- RugShader stays a Fragment sibling of `.rr-canvas` at mat level — nesting it in the canvas leaves 114px gaps top/bottom. ANOMALIES.md → "RugShader is a mat-level sibling, not a canvas child"
- Keep `#cards.mat { background-image: none }` plus the `#cards::before` redrawn grid — the only way to layer the grid above the z:0 shader. ANOMALIES.md → "Grid is promoted to `::before`"
- `BASE_Y` card-fan offsets in CardFan.tsx are Figma-matched hand-tuned values — do not normalize to a curve. ANOMALIES.md → "Card fan vertical stagger (`BASE_Y`)"
- Keep `.rr-cards-tab` typography inlined at `'wght' 800` instead of `.t-btn1` (720) — lighter weight exposes Chrome's dotted-vs-solid underline width mismatch. ANOMALIES.md → "Tab typography is inlined, not `.t-btn1`"
- Deck-fan hand overlay must always clip past the mat bottom (generous negative bottom); never let it float inside the mat or set `.rr-mat--secondary` overflow to visible. ANOMALIES.md → "Hand overlay always clips at mat bottom"
- Story card's backseat recede uses standalone CSS `scale:`, never `transform: scale()` — Framer Motion's inline transform on the same element would override it. ANOMALIES.md → "Overlay backseat choreography"
- `useExpand` stays in-flow — `<dialog>`/`showModal()` was tried and reverted; the rAF deferral on its click-outside listener is load-bearing. ANOMALIES.md → "Overlay primitive — `useExpand` (in-flow, not `<dialog>`)"
- `is-overlay-open` body class couples `useExpand` (setter) and `useDominanceSnap.maybeSnap` (reader) — rename or delete both sides together. ANOMALIES.md → "`is-overlay-open` body class — cross-file coupling"
- Intro.tsx's `setTimeout(markEnlargedClosed, 550)` must equal the CSS close-cascade sum — change both sides together. ANOMALIES.md → "Close-cascade timing — JS setTimeout matches CSS animation sum"
- Outcome ticker: never reintroduce CSS `@keyframes` on the track; the coordinate fold needs both `scrollLeft += -trackX` on first scroll and `trackX = -(scrollLeft mod segW)` on idle, velocity 0 throughout scrolling. ANOMALIES.md → "Outcome ticker — unified translate + scrollLeft"
- Ticker hover-out keeps `CRUISE_SPRING` (~12% overshoot) — documented user-requested deviation, shared with /marks autoScroll; any retune affects both. ANOMALIES.md → "Intentional overshoot — deviation from `bounce: 0`"
- Keep `overscroll-behavior-x: contain` on `.rr-outcome-ticker`. ANOMALIES.md → "`overscroll-behavior-x: contain`"
- RR cards use route-scoped `--rr-card-shadow-*` tokens, never the global elevation ladder — globals are calibrated for cream paper, these for the dark rug shader. ANOMALIES.md → "RR card shadow tokens — route-scoped, not the global ladder"
- RugShader GLSL palette is coupled to rr.css card surfaces, text contrast, and shadow alphas — changing any color requires re-checking all three. ANOMALIES.md → "RugShader palette is CSS-coupled"
- Mobile nav-sled override `.route-rr .nav-sled` stays; delete only if the mat margin returns to `-sheet-bleed`; add no other per-route marker positioning. ANOMALIES.md → "Nav-sled left — route-scoped override on mobile"
- Mobile cards canvas transform keeps its `!important` and its term split (scaled px inside the calc's first term, physical lift outside) — don't merge them. ANOMALIES.md → "Cards — whole canvas scaled to viewport"
- The interface tab never escapes the scaled cards canvas — constant chrome between tabs is the contract; recompute the accordion's max-height if notes canvas-y, pill height, or scale change. ANOMALIES.md → "Cards — interface tab stays inside the scaled canvas"
- Mobile mechanics z ladder is fixed and tight (board 4 / open rail 5 / tab 6) and the mobile matchMedia gates must stay — any new positioned sibling or reinstated JS affordance fights the flowed layout. ANOMALIES.md → "Mechanics — rails: tab peeks past board's right edge; opens onto board"
- Intro mobile North Star's `margin-top: calc(-123px + var(--space-8))` is paired with `HIDDEN_OPEN_H` in Intro.tsx and its `z-index: 1` stays below constraints card's 3 — recompute/reorder both together. ANOMALIES.md → "Intro storycard — biconomy-style natural flow"
- StoryCard `measure()` divides offsets by the scale ratio and the SVG keeps vbX=0 — removing either misplaces the dotted path; keep the ResizeObserver. ANOMALIES.md → "Dotted path (StoryCard) — scale-corrected measurement"
- `.rr-mat--secondary`'s mobile height formula is locked to the storycard's 384×688 aspect — changing card dimensions requires recomputing both the scale denominator and the height calc. ANOMALIES.md → "Post-game storycard mat — scaled card, re-anchored deck-fan"
- Keep `.rr-canvas .img__inner { opacity: 1; animation: none }` — the Img materialize keyframe stalls under useMatSettle's scroll-linked transform ancestors. ANOMALIES.md → "Img materialize animation stalls inside `.rr-canvas`"
- `object-fit` for CardFan and interface images goes on `.img.<class> .img__inner` — setting it on the wrapper span is a no-op. ANOMALIES.md → "CardFan + interface image — `object-fit` lives on `.img__inner`"
- Never give the deck-fan Img `placeholder='color'` — the asset is transparent; a color placeholder bleeds a dark rectangle through every pixel. ANOMALIES.md → "`rr-hand-deck-fan.webp` placeholder is auto-resolved"
- Mechanics card tab-toggle rotation lives on the standalone `rotate:` property (deck-fan slide-out on `translate:`) so both compose with useMatSettle's inline transform. ANOMALIES.md → "Mechanics card sheet rotation uses `rotate:`, not `transform:`"
- Rules first-visit auto-open keeps its single-fire IntersectionObserver (threshold 0.6) — scroll listeners fire continuously and re-open after dismissal. ANOMALIES.md → "RulesRail first-visit open is gated on `.rr-game-board` intersection"
- `.rr-canvas--outcome` stays at height 860px (not 900) — the trim lifts the bottom ticker into the reading band. ANOMALIES.md → "Outcome canvas height override (860 vs 900)"
- The 'only test' underline and the descent to North Star are one arc-length-parameterized SVG path — `text-decoration` suppressed on the link, or a second misaligned dotted line stacks on the SVG. ANOMALIES.md → "StoryCard one-trail unification (underline + descent)"
- `.rr-rules-rail__tab-inner` keeps `text-decoration: none` and `::after { display: none }` — under `writing-mode: vertical-lr`, t-btn1's decoration renders as a stray vertical line. ANOMALIES.md → "Rules-rail vertical tab — t-btn1 underline suppressed"
- Constraints-card title keeps `line-height: 1.3` with asymmetric padding `8px 0 4px` — Google Sans Flex caps sit high; symmetric padding reads top-aligned. ANOMALIES.md → "Constraints-card title vertical centering"
- The three rr decorative fonts stay `next/font/local` with `preload: false` — never reintroduce a fonts.googleapis.com link. ANOMALIES.md → "Decorative fonts — local, not external"
- Do not reinstate any rejected approach: mat resize model, per-session scroll lock at p=1, flex-centering of the mech family, CSS-keyframe ticker, Framer drag for the mobile rules strip, sticky full-width rules caption, `<dialog>` overlays, mount-time setTimeout for rules auto-open. ANOMALIES.md → "Rejected approaches"
